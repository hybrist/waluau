
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/interactions.jsonl

/target/
node_modules/
dist/
# The VS Code workspace extension ships as a committed self-contained bundle
# so installing it needs no build step.
!.vscode/extensions/waluau/dist/
.pnpm-debug.log
.vercel
apps/playground/public/*.wasm

# Beads / Dolt files (added by bd init)
.beads/proxieddb/
