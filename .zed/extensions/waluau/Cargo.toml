[package]
name = "waluau-zed"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"

# Keep the independently-built Zed WebAssembly extension out of the compiler's
# top-level Cargo workspace.
[workspace]
