-- Core builtin functions
-- These are host functions that provide basic runtime functionality

-- Print a string to stdout
declare function print(message: string): unit

-- Low-level string search primitive backing `string.find`/`string:find`.
-- Returns the 0-based start position of `needle` in `haystack` searching from
-- `init`, or -1 if not found. `plain` selects plain substring search (patterns
-- are not yet supported, so callers should always pass `true`).
declare function string_find(haystack: string, needle: string, init: i32, plain: bool): i32