-- Core builtin functions
-- These are host functions that provide basic runtime functionality

-- Print a string to stdout
declare function print(message: string): unit

-- String find function with Lua-compatible API
-- Returns NotFound or Found with start position (0-based indexing)
declare function string_find(haystack: string, needle: string): NotFound(unit) | Found(u32)