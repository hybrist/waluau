-- Declared host methods on extern types dispatch through host imports.

local window = require("dom:window")
local document: Document = window.document

local el: Element = document:create_element("div")
el.id = "my-id"
assert(el.id == "my-id")
