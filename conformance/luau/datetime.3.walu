-- Imported verbatim from the Luau conformance suite (luau-lang/luau),
-- tests/conformance/datetime.luau at commit 86d2a9d.
-- See README.md and LICENSE in this directory for provenance and licensing.
--
-- conformance: pending
-- The browser's Date represents pre-epoch instants, so Waluau's os.date
-- formats a negative time instead of answering nil, and its result type is a
-- plain `string`. See DEVIATIONS.md, "Browser clocks and calendar values".
-- Remove this directive only if that decision changes.
--
-- Split from conformance/luau/datetime.walu in original source order.
-- Upstream source line 19.

assert(os.date("", -1) == nil)
