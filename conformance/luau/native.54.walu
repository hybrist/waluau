-- Imported verbatim from the Luau conformance suite (luau-lang/luau),
-- tests/conformance/native.luau at commit 86d2a9d.
-- See README.md and LICENSE in this directory for provenance and licensing.
--
-- conformance: pending
-- This chunk contains only native.luau checks that currently fail in Waluau.
-- Remove this directive when the whole chunk passes.
--
-- Split from conformance/luau/native.walu. Each numbered chunk preserves a
-- contiguous section of the original test body so the conformance runner can
-- classify currently passing and pending native checks separately.

local function splitStoreRestore(x)
  local c1; c1 = function() return 1 + x end
  local c2; c2 = function() return 2 + x end
  local c3; c3 = function() return 3 + x end
  local c4; c4 = function() return 4 + x end
  local c5; c5 = function() return 5 + x end
  local c6; c6 = function() return 6 + x end
  local c7; c7 = function() return 7 + x end
  local c8; c8 = function() return 8 + x end
  local c9; c9 = function() return 9 + x end
  local ca; ca = function() return 10 + x end
  local cb; cb = function() return 11 + x end
  local cc; cc = function() return 12 + x end
  local cd; cd = function() return 13 + x end
  local ce; ce = function() return 14 + x end
  local cf; cf = function() return 15 + x end

  local t = {}

  t.a1 = c1
  t.a2 = c2
  t.a3 = c3
  t.a4 = c4
  t.a5 = c5
  t.a6 = c6
  t.a7 = c7
  t.a8 = c8
  t.a9 = c9
  t.aa = ca
  t.ab = cb
  t.ac = cc
  t.ad = cd
  t.ae = ce
  t.af = cf

  assert(is_native())
  return t
end

assert(splitStoreRestore(2).ad() == 15)
