-- Imported verbatim from the Luau conformance suite (luau-lang/luau),
-- tests/conformance/apicalls.luau at commit 86d2a9d.
-- See README.md and LICENSE in this directory for provenance and licensing.
--
-- conformance: out-of-scope: embedding-hooks
-- This upstream test exercises dynamic Luau semantics that Waluau's typed
-- subset does not support yet, so it currently fails to compile. The runner
-- only checks that a pending test currently fails (it does not care how).
-- Remove this directive if/when Waluau grows enough of the language for this
-- file to compile and instantiate cleanly.

-- This file is part of the Luau programming language and is licensed under MIT License; see LICENSE.txt for details
print('testing function calls through API')

function add(a, b)
  return a + b
end

local m = { __eq = function(a, b) return a.a == b.a end }

function create_with_tm(x)
  return setmetatable({ a = x }, m)
end

local gen = 0
function incuv()
  gen += 1
  return gen
end

pi = 3.1415926
function getpi()
  return pi
end

function largealloc()
  table.create(1000000)
end

function oops()
  return "oops"
end

function getnresults(n)
  return table.unpack(table.create(n, 1))
end

return('OK')
