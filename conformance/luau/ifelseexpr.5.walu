-- Imported verbatim from the Luau conformance suite (luau-lang/luau),
-- tests/conformance/ifelseexpr.luau at commit 86d2a9d.
-- See README.md and LICENSE in this directory for provenance and licensing.
--
-- Split from conformance/luau/ifelseexpr.walu in original source order.
-- Upstream source lines 56-59.
-- Required upstream helper setup from line 16-22 is repeated for standalone execution.

function True()
  return true
end

function False()
  return false
end
-- Test expression using non-constant condition
assert(if True() then true else false)
assert(if False() then false else true)
