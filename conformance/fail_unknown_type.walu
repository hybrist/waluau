-- A fail test: this file must NOT compile. The annotation references a type
-- that does not exist, so type checking reports an "unknown type" diagnostic.
-- The runner verifies the failure message contains the fragment(s) below.
-- conformance: error=unknown type 'DefinitelyNotAType'

local value: DefinitelyNotAType = 1
assert(value == 1)
