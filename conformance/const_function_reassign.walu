-- `const function f` declares a non-rebindable binding: assigning another
-- function to the name must fail.
-- conformance: error=cannot rebind const local 'f'
function entry(): f64
    const function f(): f64
        return 1.0
    end
    f = function(): f64
        return 2.0
    end
    return f()
end
