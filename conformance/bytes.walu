-- Bytes support: literals materialize as host-managed binary values.
function entry(data: bytes): i32
    local prefix: bytes = b"AB"
    local merged: bytes = prefix .. data
    if merged == b"AB" then
        return 0
    end
    if merged < b"\xFF" then
        return merged[0] + #merged
    end
    return 0
end
