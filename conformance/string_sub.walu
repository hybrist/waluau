-- string.sub / string:sub use 0-based inclusive indices in Waluau.

local message: string = "abcdef"

assert(string.sub(message, 1, 3) == "bcd")
assert(message:sub(2) == "cdef")
assert(string.sub(message, -3, -1) == "def")
