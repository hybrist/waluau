type ProbeBox = { value: i32 }

local function inner(value: i32): i32
    local boxed: ProbeBox = { value = value + 1 }
    return boxed.value
end

local function trap_inner(): unit
    assert(false, "DWARF compiler probe trap")
end

return {
    inner = inner,
    trap_inner = trap_inner,
}
