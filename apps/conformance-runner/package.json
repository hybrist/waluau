{
  "name": "conformance-runner",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:browser": "vitest run --browser.enabled"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^4.0.8",
    "vite": "^8.0.14",
    "vitest": "^4.0.8"
  }
}
